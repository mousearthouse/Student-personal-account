import './loginPage.scss';
import { useState, useEffect, useCallback } from "react";

const LoginPage = () => {

    return (
        <main>
            <div> логин пейдж
            </div>
        </main>
    );
};

export default LoginPage;