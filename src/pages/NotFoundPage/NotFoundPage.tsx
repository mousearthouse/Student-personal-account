import tape from '@/assets/404-tape.svg';

const NotFoundPage = () => {

    return (
        <main>
            <div> 404
            </div>
            <img src={tape} alt="Logo" height="400px" />
        </main>
    );
};

export default NotFoundPage;